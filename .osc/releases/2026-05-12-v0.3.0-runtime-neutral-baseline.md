# Release / Evidence Note: v0.3.0 — Runtime-neutral semi-autonomous protocol baseline

Date: 2026-05-12

## Summary

`v0.3.0` is the first Open Scaffold product baseline where the project is more than a template: it defines a runtime-neutral task/run/evidence protocol for semi-autonomous development and proves it can develop itself through the same chain.

## Included baseline

- Task/run/operator-surface identity model.
- GitHub issue/PR/Codex review loop.
- Slice close evidence protocol.
- Glass cockpit event protocol.
- Runtime harness dispatch pattern.
- Runtime binding contract.
- Self-dogfood issue -> plan -> run ID -> PR -> Codex -> verification -> release evidence proof.
- Official scaffold-native `.osc/releases/` evidence notes.
- Local stale-state/release-evidence validation checks.

## Traceability

- PR #7: Runtime binding contract.
- Issue #8 / PR #9: Self-dogfood proof chain.
- Current slice: v2 roadmap/stale-state/release baseline.
- Tag: pending until PR merge.
- GitHub Release: pending until tag/release publication.

## Verification

Pre-PR local verification for the v0.3.0 baseline passed:

```text
./verify.sh --standard        -> pass
./verify.sh --strict          -> 10 pass, 0 fail, 0 warn
npm run osc -- verify         -> PASS mission defined and 14 plan file(s) found; 0 warning(s)
npm test                      -> 3 files / 9 tests passed
npm run build                 -> TypeScript build passed
git diff --check              -> passed
```

## Outcome

```text
pending until tag/release publication
```

## Follow-up roadmap

- M7: CLI validation upgrades.
- M8: User-facing examples.
- M9: Runtime harness adapter refresh.
- M10: Product packaging and release polish.
